language: node_js
script: nvm install node
node_js:
 — “node”
install: npm install
deploy:
 provider: pages
 skip-cleanup: true
 github-token: $GITHUB_TOKEN 
 keep-history: true
 on:
   branch: master
 local_dir: www

